
/**
 * Frontend script for <%= block.displayName %> Block
 *
 * This file is still empty and it doesn't get enqueued yet.
 * To enqueue, edit:
 * - <%= block.class_name %>::register_block
 * - <%= block.class_name %>::enqueue_assets_frontend
 *
 * // Enqueued in frontend only
 */

// // access localized data
// const localizedData = <%= block.handle %>_data;

